<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.hitch.mapper.AiFilesMapper">
    
    <resultMap type="AiFiles" id="AiFilesResult">
        <result property="id"    column="id"    />
        <result property="name"    column="name"    />
        <result property="url"    column="url"    />
    </resultMap>

    <sql id="selectAiFilesVo">
        select id, name, url from t_ai_files
    </sql>

    <select id="selectAiFilesList" parameterType="AiFiles" resultMap="AiFilesResult">
        <include refid="selectAiFilesVo"/>
        <where>  
            <if test="name != null  and name != ''"> and name like concat('%', #{name}, '%')</if>
            <if test="url != null  and url != ''"> and url = #{url}</if>
        </where>
    </select>
    
    <select id="selectAiFilesById" parameterType="Long" resultMap="AiFilesResult">
        <include refid="selectAiFilesVo"/>
        where id = #{id}
    </select>
        
    <insert id="insertAiFiles" parameterType="AiFiles" useGeneratedKeys="true" keyProperty="id">
        insert into t_ai_files
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="name != null">name,</if>
            <if test="url != null">url,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="name != null">#{name},</if>
            <if test="url != null">#{url},</if>
         </trim>
    </insert>

    <update id="updateAiFiles" parameterType="AiFiles">
        update t_ai_files
        <trim prefix="SET" suffixOverrides=",">
            <if test="name != null">name = #{name},</if>
            <if test="url != null">url = #{url},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteAiFilesById" parameterType="Long">
        delete from t_ai_files where id = #{id}
    </delete>

    <delete id="deleteAiFilesByIds" parameterType="String">
        delete from t_ai_files where id in 
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper>